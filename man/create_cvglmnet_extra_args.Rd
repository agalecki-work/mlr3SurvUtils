% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_cvglmnet_extra_args.R
\name{create_cvglmnet_extra_args}
\alias{create_cvglmnet_extra_args}
\title{Create Extra Arguments for cv.glmnet from Training Partition}
\usage{
create_cvglmnet_extra_args(task, nfolds = 5, seed = 123, traceon = FALSE)
}
\arguments{
\item{task}{A task object from which a training partition is designated. The task is expected to have properties such as number of rows (\code{nrow}), column roles (\code{col_roles}), strata, row IDs (\code{row_ids}), and feature names (\code{feature_names}).}

\item{nfolds}{An integer indicating the number of folds to use in cross-validation. Default is 5.}

\item{seed}{An integer seed for random number generation to ensure reproducibility. Default is 123.}

\item{traceon}{Logical. If \code{TRUE}, enables detailed tracing output for debugging. Default is \code{FALSE}.}
}
\value{
A list containing:
\itemize{
\item \code{foldid}: An integer vector assigning each observation to a fold for cross-validation.
\item \code{penalty.factor}: A named numeric vector indicating penalty factors for the features, with default values of 1 for each feature.
}
}
\description{
This function prepares additional arguments for the \code{cv.glmnet} function, focusing on a designated training partition of a task. It generates fold IDs for cross-validation and penalty factors for features.
}
\examples{
\dontrun{
# Assuming `task` represents a dataset with a training partition
extra_args <- create_cvglmnet_extra_args(task, nfolds = 10, seed = 456)
print(extra_args$foldid)
print(extra_args$penalty.factor)
}

}
